import React from 'react';
import Login from '../Login/Login';

const Header = () => {
  return (
      <header>
        <h1>Movie Tracker</h1>
        <Login />
      </header>
  )
}

export default Header;